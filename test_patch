diff --git a/entry/build-profile.json5 b/entry/build-profile.json5
index bcc44dc..6fa252f 100644
--- a/entry/build-profile.json5
+++ b/entry/build-profile.json5
@@ -12,7 +12,7 @@
     }
   ],
   "targets": [
-    { "name": "default" }
+    { "name": "default" },
+    { "name": "ohosTest" }
   ]
 }
-
diff --git a/entry/src/main/ets/pages/IssueHomePage.ets b/entry/src/main/ets/pages/IssueHomePage.ets
index 7148304..cf62a71 100644
--- a/entry/src/main/ets/pages/IssueHomePage.ets
+++ b/entry/src/main/ets/pages/IssueHomePage.ets
@@ -53,6 +53,7 @@ struct IssueHomePage {
     .padding(0)
     .backgroundColor(Color.White)
     .borderRadius(12)
+    .id(`issue-card-${index}`)
     .onClick(() => {
       router.pushUrl({ url: url });
     })
diff --git a/entry/src/ohosTest/ets/test/Ability.test.ets b/entry/src/ohosTest/ets/test/Ability.test.ets
new file mode 100644
index 0000000..85c78f6
--- /dev/null
+++ b/entry/src/ohosTest/ets/test/Ability.test.ets
@@ -0,0 +1,35 @@
+import { hilog } from '@kit.PerformanceAnalysisKit';
+import { describe, beforeAll, beforeEach, afterEach, afterAll, it, expect } from '@ohos/hypium';
+
+export default function abilityTest() {
+  describe('ActsAbilityTest', () => {
+    // Defines a test suite. Two parameters are supported: test suite name and test suite function.
+    beforeAll(() => {
+      // Presets an action, which is performed only once before all test cases of the test suite start.
+      // This API supports only one parameter: preset action function.
+    })
+    beforeEach(() => {
+      // Presets an action, which is performed before each unit test case starts.
+      // The number of execution times is the same as the number of test cases defined by **it**.
+      // This API supports only one parameter: preset action function.
+    })
+    afterEach(() => {
+      // Presets a clear action, which is performed after each unit test case ends.
+      // The number of execution times is the same as the number of test cases defined by **it**.
+      // This API supports only one parameter: clear action function.
+    })
+    afterAll(() => {
+      // Presets a clear action, which is performed after all test cases of the test suite end.
+      // This API supports only one parameter: clear action function.
+    })
+    it('assertContain', 0, () => {
+      // Defines a test case. This API supports three parameters: test case name, filter parameter, and test case function.
+      hilog.info(0x0000, 'testTag', '%{public}s', 'it begin');
+      let a = 'abc';
+      let b = 'b';
+      // Defines a variety of assertion methods, which are used to declare expected boolean conditions.
+      expect(a).assertContain(b);
+      expect(a).assertEqual(a);
+    })
+  })
+}
\ No newline at end of file
diff --git a/entry/src/ohosTest/ets/test/List.test.ets b/entry/src/ohosTest/ets/test/List.test.ets
new file mode 100644
index 0000000..e80d68e
--- /dev/null
+++ b/entry/src/ohosTest/ets/test/List.test.ets
@@ -0,0 +1,7 @@
+import failToPassTest from './failtopass.test';
+import passToPassTest from './passtopass.test';
+
+export default function testsuite() {
+  passToPassTest();
+  failToPassTest();
+}
diff --git a/entry/src/ohosTest/ets/test/TestHelper.ets b/entry/src/ohosTest/ets/test/TestHelper.ets
new file mode 100644
index 0000000..66bf74f
--- /dev/null
+++ b/entry/src/ohosTest/ets/test/TestHelper.ets
@@ -0,0 +1,130 @@
+import { Want } from '@kit.AbilityKit';
+import { window } from '@kit.ArkUI';
+import { abilityDelegatorRegistry, Component, Driver, ON } from '@kit.TestKit';
+import { expect } from '@ohos/hypium';
+
+export const SETTINGS_CARD_ID: string = 'issue-card-01';
+export const LOGOUT_LABEL: string = '退出登录';
+export const LAST_MENU_LABEL: string = '检查更新';
+
+interface FontSample {
+  text: string;
+  fontSizeVp: number;
+}
+
+const FONT_SAMPLES: FontSample[] = [
+  { text: '账号与安全', fontSizeVp: 13.5 },
+  { text: '通用设置', fontSizeVp: 13.5 },
+  { text: '免打扰 22:00-次日08:00', fontSizeVp: 11.5 }
+];
+
+export async function startAbility(driver: Driver, bundleName: string): Promise<void> {
+  const want: Want = { bundleName: bundleName, abilityName: 'EntryAbility' };
+  await abilityDelegatorRegistry.getAbilityDelegator().startAbility(want);
+  await driver.waitForIdle(2000, 3000);
+}
+
+export async function openSettingsPage(driver: Driver): Promise<void> {
+  if (await driver.findComponent(ON.text('设置')) !== null) {
+    await driver.pressBack();
+    await driver.waitForIdle(1000, 2000);
+  }
+  const settingsCard = await driver.findComponent(ON.id(SETTINGS_CARD_ID));
+  expect(settingsCard !== null).assertTrue();
+  if (settingsCard !== null) {
+    await settingsCard.click();
+    await driver.waitForIdle(2000, 3000);
+  }
+}
+
+export async function findRequiredText(driver: Driver, text: string): Promise<Component> {
+  const component = await driver.findComponent(ON.text(text));
+  expect(component !== null).assertTrue();
+  return component;
+}
+
+export async function findLogoutButton(driver: Driver, bundleName: string): Promise<Component> {
+  let component = await driver.findComponent(ON.text(LOGOUT_LABEL));
+  if (component !== null) {
+    return component;
+  }
+
+  const window = await driver.findWindow({ bundleName: bundleName, active: true });
+  const windowBounds = await window.getBounds();
+  const centerX = Math.round((windowBounds.left + windowBounds.right) / 2);
+  const startY = Math.round(windowBounds.bottom - 80);
+  const endY = Math.round(windowBounds.top + 80);
+
+  for (let index = 0; index < 5; index++) {
+    await driver.swipe(centerX, startY, centerX, endY, 800);
+    await driver.waitForIdle(1000, 2000);
+    component = await driver.findComponent(ON.text(LOGOUT_LABEL));
+    if (component !== null) {
+      return component;
+    }
+  }
+
+  expect(false).assertTrue();
+  return component;
+}
+
+export async function scrollSettingsToBottom(driver: Driver): Promise<void> {
+  const scrollables = await driver.findComponents(ON.scrollable(true));
+  console.info(`settings scrollable areas: ${scrollables.length}`);
+  for (const scrollable of scrollables) {
+    await scrollable.scrollToBottom(800);
+    await driver.waitForIdle(1000, 2000);
+  }
+}
+
+export async function verifyMenuAndLogoutDoNotOverlap(driver: Driver): Promise<void> {
+  try {
+    await scrollSettingsToBottom(driver);
+  } catch (e) {
+    console.warn('Could not confirm scroll bottom; checking current bounds: ' + JSON.stringify(e));
+  }
+  await driver.waitForIdle(1000, 2000);
+  const logout = await driver.findComponent(ON.id('settings-logout-button'));
+  const lastMenuItem = await driver.findComponent(ON.id('settings-menu-item-10'));
+  if (logout === null || lastMenuItem === null) {
+    throw new Error('LAYOUT_CHECK_ERROR: cannot locate logout button or last menu item; overlap is unknown');
+  }
+  const logoutBounds = await logout.getBounds();
+  const lastMenuItemBounds = await lastMenuItem.getBounds();
+
+  console.info(`layout check: logout=${JSON.stringify(logoutBounds)}, ` +
+    `last menu item=${JSON.stringify(lastMenuItemBounds)}`);
+  if (!Number.isFinite(logoutBounds.top) || !Number.isFinite(logoutBounds.bottom) ||
+    !Number.isFinite(lastMenuItemBounds.top) || !Number.isFinite(lastMenuItemBounds.bottom) ||
+    logoutBounds.bottom <= logoutBounds.top || lastMenuItemBounds.bottom <= lastMenuItemBounds.top) {
+    throw new Error('LAYOUT_CHECK_ERROR: invalid component bounds; overlap is unknown');
+  }
+  console.info(`logout/menu vertical gap: ${logoutBounds.top - lastMenuItemBounds.bottom}px`);
+  expect(logoutBounds.top >= lastMenuItemBounds.bottom).assertTrue();
+}
+
+export async function verifyFontsMatchPhoneReference(driver: Driver): Promise<void> {
+  const ability = await abilityDelegatorRegistry.getAbilityDelegator().getCurrentTopAbility();
+  const appWindow = await window.getLastWindow(ability.context);
+  const uiContext = appWindow.getUIContext();
+  const measure = uiContext.getMeasureUtils();
+
+  for (const sample of FONT_SAMPLES) {
+    const text = await findRequiredText(driver, sample.text);
+    const bounds = await text.getBounds();
+
+    const expected = measure.measureTextSize({
+      textContent: sample.text,
+      fontSize: `${sample.fontSizeVp}vp`,
+      fontWeight: 400
+    });
+    const expectedWidth = Number(expected.width);
+    const actualWidth = bounds.right - bounds.left;
+    const tolerance = Math.max(2, expectedWidth * 0.02);
+    console.info(`font reference ${sample.text}: actual=${actualWidth}px, ` +
+      `expected=${expectedWidth}px, tolerance=${tolerance}px`);
+    expect(expectedWidth > 0).assertTrue();
+    expect(actualWidth > 0).assertTrue();
+    expect(Math.abs(actualWidth - expectedWidth) <= tolerance).assertTrue();
+  }
+}
diff --git a/entry/src/ohosTest/ets/test/failtopass.test.ets b/entry/src/ohosTest/ets/test/failtopass.test.ets
new file mode 100644
index 0000000..7c1ff59
--- /dev/null
+++ b/entry/src/ohosTest/ets/test/failtopass.test.ets
@@ -0,0 +1,64 @@
+import { afterAll, beforeAll, describe, expect, it, Level } from '@ohos/hypium';
+import { abilityDelegatorRegistry, Driver } from '@kit.TestKit';
+import {
+  openSettingsPage,
+  startAbility,
+  verifyFontsMatchPhoneReference,
+  verifyMenuAndLogoutDoNotOverlap
+} from './TestHelper';
+
+export default function failToPassTest() {
+  describe('FailToPassTest', () => {
+    let driver: Driver;
+    let bundleName: string;
+
+    beforeAll(async () => {
+      try {
+        driver = Driver.create();
+        bundleName = abilityDelegatorRegistry.getArguments().bundleName;
+        await startAbility(driver, bundleName);
+        await openSettingsPage(driver);
+      } catch (e) {
+        console.error('FailToPassTest beforeAll error: ' + JSON.stringify(e));
+      }
+    })
+
+    afterAll(async () => {
+      try {
+        await driver.waitForIdle(2000, 3000);
+      } catch (e) {
+        console.error('FailToPassTest afterAll error: ' + JSON.stringify(e));
+      }
+    })
+
+    it('should_show_logout_under_menu', Level.LEVEL1, async (done: Function) => {
+      try {
+        await openSettingsPage(driver);
+        const window = await driver.findWindow({ bundleName: bundleName, active: true });
+        const bounds = await window.getBounds();
+        console.info(`verify settings layout at ${bounds.right - bounds.left}x${bounds.bottom - bounds.top}px`);
+        await verifyMenuAndLogoutDoNotOverlap(driver);
+      } catch (e) {
+        console.error('should_show_logout_under_menu error: ' + JSON.stringify(e));
+        expect(false).assertTrue();
+      } finally {
+        done();
+      }
+    })
+
+    it('should_keep_font_visual_size', Level.LEVEL1, async (done: Function) => {
+      try {
+        await openSettingsPage(driver);
+        const window = await driver.findWindow({ bundleName: bundleName, active: true });
+        const bounds = await window.getBounds();
+        console.info(`verify phone-reference fonts at ${bounds.right - bounds.left}x` +
+          `${bounds.bottom - bounds.top}px`);
+        await verifyFontsMatchPhoneReference(driver);
+      } catch (e) {
+        console.error('should_keep_font_visual_size error: ' + JSON.stringify(e));
+        expect(false).assertTrue();
+      }
+      done();
+    })
+  })
+}
diff --git a/entry/src/ohosTest/ets/test/passtopass.test.ets b/entry/src/ohosTest/ets/test/passtopass.test.ets
new file mode 100644
index 0000000..513de3d
--- /dev/null
+++ b/entry/src/ohosTest/ets/test/passtopass.test.ets
@@ -0,0 +1,81 @@
+import { afterAll, beforeAll, describe, expect, it, Level } from '@ohos/hypium';
+import { abilityDelegatorRegistry, Driver, ON } from '@kit.TestKit';
+import { window } from '@kit.ArkUI';
+import { findLogoutButton, openSettingsPage, startAbility, verifyFontsMatchPhoneReference } from './TestHelper';
+
+export default function passToPassTest() {
+  describe('PassToPassTest', () => {
+    let driver: Driver;
+    let bundleName: string;
+
+    beforeAll(async () => {
+      try {
+        driver = Driver.create();
+        bundleName = abilityDelegatorRegistry.getArguments().bundleName;
+        await startAbility(driver, bundleName);
+        await openSettingsPage(driver);
+      } catch (e) {
+        console.error('PassToPassTest beforeAll error: ' + JSON.stringify(e));
+      }
+    })
+
+    afterAll(async () => {
+      try {
+        await driver.waitForIdle(2000, 3000);
+      } catch (e) {
+        console.error('PassToPassTest afterAll error: ' + JSON.stringify(e));
+      }
+    })
+
+    it('should_start_ability_successfully', Level.LEVEL0, async (done: Function) => {
+      try {
+        let page = await driver.findComponent(ON.id('settings-page'));
+        if (page === null) {
+          page = await driver.findComponent(ON.id('settings-page-root'));
+        }
+        expect(page !== null).assertTrue();
+        await driver.delayMs(500);
+      } catch (e) {
+        console.error('should_start_ability_successfully error: ' + JSON.stringify(e));
+        expect(false).assertTrue();
+      }
+      done();
+    })
+
+    it('should_show_logout_button', Level.LEVEL0, async (done: Function) => {
+      try {
+        const logout = await findLogoutButton(driver, bundleName);
+        expect(logout !== null).assertTrue();
+      } catch (e) {
+        console.error('should_show_logout_button error: ' + JSON.stringify(e));
+        expect(false).assertTrue();
+      }
+      done();
+    })
+
+    it('should_keep_font_visual_size_on_phone', Level.LEVEL1, async (done: Function) => {
+      try {
+        const ability = await abilityDelegatorRegistry.getAbilityDelegator().getCurrentTopAbility();
+        const appWindow = await window.getLastWindow(ability.context);
+        const uiContext = appWindow.getUIContext();
+        const rect = appWindow.getWindowProperties().windowRect;
+        const widthVp = uiContext.px2vp(rect.width);
+
+        const isReferencePhoneWindow = Math.abs(widthVp - 360) <= 1 && rect.height > rect.width;
+        if (isReferencePhoneWindow) {
+          await openSettingsPage(driver);
+          console.info(`verify unchanged phone fonts at ${widthVp}vp`);
+          await verifyFontsMatchPhoneReference(driver);
+        } else {
+          console.info(`SKIP phone font comparison: requires a portrait 360vp window; actual=${widthVp}vp`);
+          expect(true).assertTrue();
+        }
+      } catch (e) {
+        console.error('should_keep_font_visual_size_on_phone error: ' + JSON.stringify(e));
+        expect(false).assertTrue();
+      } finally {
+        done();
+      }
+    })
+  })
+}
diff --git a/entry/src/ohosTest/module.json5 b/entry/src/ohosTest/module.json5
new file mode 100644
index 0000000..699613a
--- /dev/null
+++ b/entry/src/ohosTest/module.json5
@@ -0,0 +1,9 @@
+{
+  "module": {
+    "name": "entry_test",
+    "type": "feature",
+    "deviceTypes": ["phone", "tablet", "2in1"],
+    "deliveryWithInstall": true,
+    "installationFree": false
+  }
+}
diff --git a/oh-package-lock.json5 b/oh-package-lock.json5
index 2857b55..c5a91ec 100644
--- a/oh-package-lock.json5
+++ b/oh-package-lock.json5
@@ -5,6 +5,24 @@
   },
   "lockfileVersion": 3,
   "ATTENTION": "THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.",
-  "specifiers": {},
-  "packages": {}
+  "specifiers": {
+    "@ohos/hamock@1.0.0": "@ohos/hamock@1.0.0",
+    "@ohos/hypium@1.0.25": "@ohos/hypium@1.0.25"
+  },
+  "packages": {
+    "@ohos/hamock@1.0.0": {
+      "name": "@ohos/hamock",
+      "version": "1.0.0",
+      "integrity": "sha512-K6lDPYc6VkKe6ZBNQa9aoG+ZZMiwqfcR/7yAVFSUGIuOAhPvCJAo9+t1fZnpe0dBRBPxj2bxPPbKh69VuyAtDg==",
+      "resolved": "https://ohpm.openharmony.cn/ohpm/@ohos/hamock/-/hamock-1.0.0.har",
+      "registryType": "ohpm"
+    },
+    "@ohos/hypium@1.0.25": {
+      "name": "@ohos/hypium",
+      "version": "1.0.25",
+      "integrity": "sha512-l6uO2pjl8HyEKdekLqQt7tUpWbDqX/42zoAzkagtUVZAW9jT6lMvbe54MVjoLxq/RwQGygRvi6j4GpypSMFSHw==",
+      "resolved": "https://ohpm.openharmony.cn/ohpm/@ohos/hypium/-/hypium-1.0.25.har",
+      "registryType": "ohpm"
+    }
+  }
 }
\ No newline at end of file
diff --git a/oh-package.json5 b/oh-package.json5
index 77a9f46..db616fa 100644
--- a/oh-package.json5
+++ b/oh-package.json5
@@ -7,7 +7,9 @@
   "author": "",
   "license": "UNLICENSED",
   "dependencies": {},
-  "devDependencies": {},
+  "devDependencies": {
+    "@ohos/hypium": "1.0.25",
+    "@ohos/hamock": "1.0.0"
+  },
   "dynamicDependencies": {}
 }
-
